// Используйте синтаксис ESM для импорта плагинов
import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';
// Импортируйте другие плагины PostCSS, если они вам нужны

// Экспортируйте конфигурацию PostCSS
export default {
    plugins: [
        tailwindcss,
        autoprefixer,
        // Укажите другие плагины здесь
    ],
};
